// TypeScript file
class StringUtils {
	//根据名称查询获取bitmap资源
	public static numberToString(num:number):string{
		var str = "";
        var i:number;
        if(num >= 100){

        }
        return str;
	}
}